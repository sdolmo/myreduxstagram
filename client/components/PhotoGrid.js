import React from 'react';
import Link from 'react-router';

const PhotoGrid = React.createClass({
  render() {
    return (
      <div>
        Yo! we out here in the grid
      </div>
    )
  }
});

export default PhotoGrid;
